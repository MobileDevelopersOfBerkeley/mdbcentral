#!/usr/bin/env node

const run = require("../util/job.js").run;
const fn = require("../logic/Slack1.js").sendReminders;
const initLabel = "Starting to send event reminders...";
const successLabel = "Event reminders sent!";
run(fn, initLabel, successLabel);
