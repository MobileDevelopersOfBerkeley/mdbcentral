#!/usr/bin/env node

const run = require("../util/job.js").run;
const fn = require("../logic/GithubCache.js").set;
const initLabel = "Starting refresh for github cache...";
const successLabel = "Github cache refreshed!";
run(fn, initLabel, successLabel);
