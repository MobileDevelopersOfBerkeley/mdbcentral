#!/usr/bin/env node

const run = require("../util/job.js").run;
const fn = require("../logic/Slack2.js").sendReminders;
const initLabel = "Starting to send task reminders...";
const successLabel = "Task reminders sent!";
run(fn, initLabel, successLabel);
