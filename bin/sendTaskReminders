#!/usr/bin/env node

var sendReminders = require("../logic/Slack2.js").sendReminders;

console.log("Starting to send task reminders...");
sendReminders().then(function() {
  console.log("Success: Task reminders sent!");
}).catch(function(error) {
  console.error("Error: ", error);
});
